on:
  pull_request:
    branches:
    - main
  workflow_dispatch:
name: 🚀 Deploy website on pull to main
jobs:
  deployment:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    environment:
      name: Main Site
      url: http://three.tripnia.net/sab
    steps:
    - name: Get latest code
      uses: actions/checkout@v4
    
    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftpupload.net
        username: if0_37363127
        password: ${{ secrets.ftp_password }}
        server-dir: /three.tripnia.net/htdocs/sab/
